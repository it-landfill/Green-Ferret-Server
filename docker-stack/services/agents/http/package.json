{
    "name": "green-ferret-agents-http",
    "version": "1.0.0",
    "description": "HTTP Agent for Green Ferret.\nThis agent is responsible for handling the HTTP requests and responses and for uploading the received data to InfluxDB.",
    "main": "main.js",
    "scripts": {
        "watch": "tsc --watch",
        "test": "echo \"Error: no test specified\" && exit 1"
    },
    "author": "Benetton Alessandro, Crespan Lorenzo",
    "license": "GPL-3.0",
    "dependencies": {
        "@influxdata/influxdb-client": "^1.33.2"
    },
    "devDependencies": {
        "ts-node": "^10.9.1",
        "typescript": "^5.0.4"
    }
}